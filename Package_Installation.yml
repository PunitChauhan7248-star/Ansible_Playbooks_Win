---
- name: Install packages on Linux
  hosts: localhost
  become: yes

  tasks:
    - name: Install required packages
      yum:
        name:
          - curl
          - traceroute
        state: present
