---
- name: Install packages on Linux
  hosts: 10.1.0.37
  become: yes

  tasks:
    - name: Install required packages
      yum:
        name:
          - curl
          - traceroute
        state: present
